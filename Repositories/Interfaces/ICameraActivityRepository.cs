using WebCamServer.Models;

namespace WebCamServer.Repositories.Interfaces
{
  public interface ICameraActivityRepository
  {
    // Task<List<CameraActivity>> GetAll();
    // Task<CameraActivity> GetById(string id);
    // Task Create(CameraActivity newEntity);
    // Task Update(string id, CameraActivity updatedEntity);
    // Task Delete(string id);
  }
}